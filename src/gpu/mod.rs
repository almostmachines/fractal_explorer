//! GPU compute adapters (driven side).
//!
//! Implements the interactive controller's `GpuFractalRendererPort` with a
//! wgpu compute shader. Behind the `gpu` feature.

pub mod perturbation_renderer;
